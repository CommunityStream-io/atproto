/**
 * GENERATED CODE - DO NOT MODIFY
 */
import { HeadersMap, XRPCError } from '@atproto/xrpc'
import { type ValidationResult, BlobRef } from '@atproto/lexicon'
import { CID } from 'multiformats/cid'
import { validate as _validate } from '../../../../lexicons'
import {
  type $Typed,
  is$typed as _is$typed,
  type OmitKey,
} from '../../../../util'
import type * as ComAtprotoRepoStrongRef from '../../../com/atproto/repo/strongRef.js'

const is$typed = _is$typed,
  validate = _validate
const id = 'app.bsky.graph.respondToFollowRequest'

export type QueryParams = {}

export interface InputSchema {
  /** AT-URI of the follow request record to respond to */
  requestUri: string
  /** Whether to approve or deny the follow request */
  response: 'approve' | 'deny'
}

export interface OutputSchema {
  request: ComAtprotoRepoStrongRef.Main
  followRecord?: ComAtprotoRepoStrongRef.Main
}

export interface CallOptions {
  signal?: AbortSignal
  headers?: HeadersMap
  qp?: QueryParams
  encoding?: 'application/json'
}

export interface Response {
  success: boolean
  headers: HeadersMap
  data: OutputSchema
}

export class RequestNotFoundError extends XRPCError {
  constructor(src: XRPCError) {
    super(src.status, src.error, src.message, src.headers, { cause: src })
  }
}

export class InvalidResponseError extends XRPCError {
  constructor(src: XRPCError) {
    super(src.status, src.error, src.message, src.headers, { cause: src })
  }
}

export class NotAuthorizedError extends XRPCError {
  constructor(src: XRPCError) {
    super(src.status, src.error, src.message, src.headers, { cause: src })
  }
}

export function toKnownErr(e: any) {
  if (e instanceof XRPCError) {
    if (e.error === 'RequestNotFound') return new RequestNotFoundError(e)
    if (e.error === 'InvalidResponse') return new InvalidResponseError(e)
    if (e.error === 'NotAuthorized') return new NotAuthorizedError(e)
  }

  return e
}
